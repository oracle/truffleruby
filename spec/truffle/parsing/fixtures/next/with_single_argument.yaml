subject: "Next"
description: "with single returned value"
notes: >
  Operator is represented by NextNode.
  Returned value is represented by a child node (IntegerFixnumLiteralNode in the example below).
focused_on_node: "org.truffleruby.language.control.NextNode"
ruby: |
  while (true)
    next 42
  end
ast: |
  NextNode
      attributes:
          flags = 1
          sourceCharIndex = 15
          sourceLength = 7
      children:
          child =
              IntegerFixnumLiteralNode
                  attributes:
                      flags = 0
                      sourceCharIndex = 20
                      sourceLength = 2
                      value = 42