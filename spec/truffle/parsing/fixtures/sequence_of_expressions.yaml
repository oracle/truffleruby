subject: "Sequence of expressions"
description: "Multiple expressions separated with `;` or new line"
focused_on_node: "org.truffleruby.language.control.SequenceNode"
ruby: |
  42
  100500
ast: |
  SequenceNode
      attributes:
          flags = 12
      children:
          body = [
              EmitWarningsNode
                  attributes:
                      flags = 0
                      warnings = RubyDeferredWarnings(WarningMessage(message = 'Useless use of a literal in void context.', verbosity = VERBOSE, fileName = '<parse_ast>', lineNumber = 1), WarningMessage(message = 'Useless use of a literal in void context.', verbosity = VERBOSE, fileName = '<parse_ast>', lineNumber = 2))
              WriteLocalVariableNode
                  attributes:
                      flags = 0
                      frameSlot = 0 # (self)
                  children:
                      valueNode =
                          ProfileArgumentNodeGen
                              attributes:
                                  flags = 0
                              children:
                                  childNode_ =
                                      ReadSelfNode
                                          attributes:
                                              flags = 0
              IntegerFixnumLiteralNode
                  attributes:
                      flags = 1
                      value = 42
              IntegerFixnumLiteralNode
                  attributes:
                      flags = 1
                      value = 100500
          ]