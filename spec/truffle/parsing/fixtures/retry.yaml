subject: "retry"
description: "retry control flow operator"
focused_on_node: "org.truffleruby.language.control.RetryNode"
ruby: |
  begin
  rescue
    retry
  end
ast: |
  RetryNode
      attributes:
          flags = 1
          sourceCharIndex = 15
          sourceLength = 5